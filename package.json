{
  "dependencies": {
    "node": "^26.5.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  }
}
